<div>
  <%= flash[:alert] %>
</div>

<h5>ジャンル編集</h5>
<%= form_with model: @genre, url: admin_genre_path(@genre.id), local:true do |f| %>
  <div><%= f.text_field :name %></div>
  <%= f.submit '変更を保存' %>
<% end %>  